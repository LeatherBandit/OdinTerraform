Terraform files for creating an EC2 instance in AWS.
IAM access keyfile "terraform.tfvars" excluded but follows this format:

aws_access_key = "aws_access_key"
aws_secret_key = "aws_secret_key"
aws_region     = "aws_region"
